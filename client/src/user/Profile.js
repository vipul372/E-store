import React from 'react';
import {Base} from '../core/Base';

const Profile = () => {
  return (  
    <Base title="Profile">
      welcome to user profile
    </Base>
  );
}
 
export default Profile;